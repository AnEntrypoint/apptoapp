{
  "name": "apptoapp",
  "version": "1.2.4",
  "description": "Application with Copilot integration",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --detectOpenHandles --forceExit --testTimeout=120000 --maxWorkers=1 --no-cache",
    "lint": "eslint .",
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "release": "git commit -a -m ranrelease && npm version patch  && npm publish --tag latest"
  },
  "dependencies": {
    "abort-controller": "^3.0.0",
    "commander": "^13.1.0",
    "crypto": "^1.0.1",
    "dotenv": "^16.4.7",
    "fast-glob": "^3.3.3",
    "ignore": "^7.0.3",
    "jsdom": "^22.1.0",
    "node-fetch": "^2.7.0",
    "node-localstorage": "^3.0.5",
    "util": "^0.12.5"
  },
  "devDependencies": {
    "eslint": "^8.40.0",
    "jest": "^29.5.0",
    "jest-junit": "^16.0.0"
  }
}
