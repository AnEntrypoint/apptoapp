{
  "name": "apptoapp",
  "version": "0.2.9",
  "main": "index.js",
  "scripts": {
    "start": "DEBUG=codestral:* node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "git commit -a -m ranrelease && npm version patch  && npm publish --tag latest"
  },
  "dependencies": {
    "debug": "^4.3.4",
    "diff": "^5.1.0",
    "dotenv": "^16.4.1",
    "ignore": "^5.3.1"
  },
  "bin": {
    "apptoapp": "index.js"
  }
}
